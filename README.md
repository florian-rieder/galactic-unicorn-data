# Galactic Unicorn Data

Stock files for the Galactic Unicorn’s LittleFS `/data` partition: system scripts (`/system/`), shared libs (`/lib/`), games, assets, and the rest of the built-in content.

## Release

```
git tag v1.0.0
git push origin v1.0.0

```
